import React from "react";
import PropTypes from "prop-types";

const NavBar = () => {
  return (
    <nav className="navbar">
      <div className="navbar-brand">
        <div className="navbar-item">TODO App</div>
      </div>
    </nav>
  );
};

NavBar.propTypes = {};

export default NavBar;
